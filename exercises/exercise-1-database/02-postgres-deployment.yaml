apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: exercise-1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        env:
        - name: POSTGRES_DB
          value: exercise_db
        - name: POSTGRES_USER
          value: exercise_user
        - name: POSTGRES_PASSWORD
          value: exercise_password
        ports:
        - containerPort: 5432
          name: postgres
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: postgres-storage
        persistentVolumeClaim:
          claimName: postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: exercise-1
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432